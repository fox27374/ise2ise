# ISE network device exports are plaintext credential dumps. Never commit one.
*.csv

# Build output. The binaries ship as release assets, not as commits: four
# ~10 MB blobs per rebuild is a repository that grows faster than its source.
/ise2ise
/dist/

# OS cruft
.DS_Store
Thumbs.db
._*
